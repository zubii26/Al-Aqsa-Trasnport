import dbConnect from './src/lib/mongodb'; import { Vehicle } from './src/models'; async function run() { await dbConnect(); await Vehicle.updateOne({ name: 'Mercedes S-Class' }, { $set: { price: 'From SAR 700' } }); await Vehicle.updateOne({ name: 'Kia K5' }, { $set: { price: 'From SAR 220' } }); await Vehicle.updateOne({ name: 'Mitsubishi Xpander' }, { $set: { price: 'From SAR 280' } }); await Vehicle.updateOne({ name: 'Large Coach Bus' }, { $set: { price: 'From SAR 1000' } }); console.log('Done!'); process.exit(0); } run();
